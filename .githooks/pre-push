#!/bin/sh
# Pre-push hook: run fmt and clippy before pushing

set -e

echo "  running pre-push checks"

echo "  fmt"
cargo fmt --check

echo "  clippy"
cargo clippy --workspace --all-targets -- -D warnings -W missing_docs

echo "  checks passed"
